{
  "name": "libvim",
  "version": "8.10869.0",
  "description": "Standalone vim library",
  "license": "MIT",
  "esy": {
    "build": [
        ["bash", "-c", "#{os == 'windows' ? 'build/build-windows.sh' : 'build/build-posix.sh'}"]
    ],
    "buildsInSource":true
  },
  "dependencies": { }
}
