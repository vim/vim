This directory contains the auto-generated protocol files for Wayland.

To re-generate them run make.

It requires wayland-scanner to be installed, which is generally found as a
wayland-utils package in Linux distributions.

Included as of Vim patch XXXX.

Initial work done by Foxe Chen.
